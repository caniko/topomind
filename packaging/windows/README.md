# Windows packaging

Install the addon archive under FreeCAD's user `Mod` directory and place the
`topomind.exe` sidecar on `PATH`. The bridge uses an ephemeral loopback TCP
endpoint with the same private rendezvous record and HMAC proof as Unix
systems. The public interface remains MCP over stdio.
